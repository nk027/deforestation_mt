
# p(y|theta) = log(|A|) - (ESS / (2 * sigma)) + p(rho)

# V(a) = 1 / (S - 1) * sum((a - mean(a)) ^ 2)

WAIC <- function(x) {

  lppd <- log(mean(exp(ll)))

  p_WAICaic <- sum((ll - mean(ll)) ^ 2) / (length(ss) - 1)

  elppd <- lppd - p_WAIC

  -2 * elppd


}
